import _dicttools as dicttools
import _itertools as itertools
import _repr as repr
import _types as types

if __name__ == '__main__':
    import doctest
    doctest.testmod(dicttools)
    doctest.testmod(itertools)
    doctest.testmod(repr)
    doctest.testmod(types)
